% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bandwidth_sgd.R
\name{estimate_bandwidth_gradient}
\alias{estimate_bandwidth_gradient}
\title{Estimate the bandwidth gradient via finite differences}
\usage{
estimate_bandwidth_gradient(
  context,
  bandwidth,
  delta,
  minibatch,
  scorer,
  lower = NULL,
  upper = NULL
)
}
\arguments{
\item{context}{A `gwpr_context` list.}

\item{bandwidth}{Numeric scalar; current bandwidth.}

\item{delta}{Numeric scalar > 0; finite-difference step size.}

\item{minibatch}{Character vector of unit IDs (ignored by the default
scorer; kept for future mini-batch-aware scorers).}

\item{scorer}{A function `scorer(context, bandwidth)` that returns a
named list with at minimum a `score` field.}

\item{lower}{Numeric lower bound (or `NULL`).}

\item{upper}{Numeric upper bound (or `NULL`).}
}
\value{
A numeric scalar gradient estimate.
}
\description{
Approximates the gradient of the scorer objective with respect to bandwidth
using a central finite difference:
}
\details{
```
gradient = (f(b + delta) - f(b - delta)) / (2 * delta)
```

Both evaluations use the same `minibatch`.  When either candidate falls
outside `[lower, upper]` the function falls back to a one-sided difference
or clips the candidate to the boundary.
}
\examples{
\donttest{
grad <- estimate_bandwidth_gradient(
  context   = ctx,
  bandwidth = 10,
  delta     = 1,
  minibatch = character(),
  scorer    = my_scorer
)
}

}
