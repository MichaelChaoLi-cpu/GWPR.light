% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{validate_inputs}
\alias{validate_inputs}
\title{Validate all inputs to the main GWPR functions}
\usage{
validate_inputs(
  formula,
  data,
  spatial,
  id,
  time,
  family = c("gaussian", "binomial"),
  model = c("within", "pooling", "random"),
  effect = c("individual", "time", "two-way", "nested"),
  kernel = c("bisquare", "gaussian", "exponential", "tricube", "boxcar"),
  adaptive = FALSE,
  workers = 1L
)
}
\arguments{
\item{formula}{A formula object.}

\item{data}{A data frame.}

\item{spatial}{An `sf` object.}

\item{id}{Name of the unit ID column (character).}

\item{time}{Name of the time column (character).}

\item{family}{`"gaussian"` or `"binomial"`.}

\item{model}{`"pooling"`, `"within"`, or `"random"`.}

\item{effect}{`"individual"`, `"time"`, `"two-way"`, or `"nested"`.}

\item{kernel}{One of `"bisquare"`, `"gaussian"`, `"exponential"`,
`"tricube"`, `"boxcar"`.}

\item{adaptive}{Logical.}

\item{workers}{Positive integer.}
}
\value{
Invisibly returns `TRUE` when all checks pass; stops otherwise.
}
\description{
This is the top-level validation entry point.  It calls the individual
`validate_*` helpers and also checks `model`, `effect`, and `kernel`.
}
\keyword{internal}
