% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bandwidth_sgd.R
\name{initialize_bandwidth}
\alias{initialize_bandwidth}
\title{Initialise a bandwidth value for SGD}
\usage{
initialize_bandwidth(control, adaptive, seed = NULL)
}
\arguments{
\item{control}{A list that **must** contain `lower` and `upper`.}

\item{adaptive}{Logical.  When `TRUE` an integer bandwidth is returned.}

\item{seed}{Integer random seed or `NULL`.}
}
\value{
A numeric scalar (positive, and an integer when `adaptive = TRUE`).
}
\description{
Randomly initialises a starting bandwidth within `[lower, upper]`.  For
fixed bandwidth the initialisation is done on the log scale so the
distribution is spread across orders of magnitude.  For adaptive bandwidth
the result is rounded to a positive integer.
}
\details{
Both `lower` and `upper` **must** be supplied when calling this function.
}
\examples{
initialize_bandwidth(list(lower = 1, upper = 100), adaptive = FALSE,
                     seed = 42L)
initialize_bandwidth(list(lower = 3, upper = 20), adaptive = TRUE,
                     seed = 1L)

}
