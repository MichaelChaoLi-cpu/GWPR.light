% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bandwidth_grid.R
\name{search_bandwidth_grid}
\alias{search_bandwidth_grid}
\title{Bandwidth grid search}
\usage{
search_bandwidth_grid(context, control, scorer, workers = 1L, seed = NULL)
}
\arguments{
\item{context}{A `gwpr_context` list.  Must contain at minimum `adaptive`.}

\item{control}{A list with grid search parameters.  **Required** fields:
\describe{
  \item{`lower`}{Numeric.  Lower bound of the search grid (inclusive).}
  \item{`upper`}{Numeric.  Upper bound (included when reachable by
    `step`).}
  \item{`step`}{Numeric > 0.  Increment between candidates.}
}}

\item{scorer}{A function with signature `scorer(context, bandwidth)`
that returns a named list as described in `score_bandwidth_candidate()`.}

\item{workers}{Integer.  Number of parallel workers.  `1` (default) uses
serial `lapply`; `> 1` uses `parallel_map()`.}

\item{seed}{Integer random seed, or `NULL`.  Applied before parallel
execution.}
}
\value{
A `gwpr_bandwidth` object (see `new_gwpr_bandwidth()`).
}
\description{
Exhaustively evaluates every bandwidth candidate in the grid defined by
`control$lower`, `control$upper`, and `control$step`.  Candidates are
scored by the user-supplied `scorer` function.  The complete history and
the best bandwidth are returned as a `gwpr_bandwidth` object.
}
\examples{
\donttest{
result <- search_bandwidth_grid(
  context = ctx,
  control = list(lower = 1, upper = 5, step = 1),
  scorer  = my_scorer_fn,
  workers = 1
)
result$best_bandwidth
}

}
