% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel.R
\name{validate_parallel_result}
\alias{validate_parallel_result}
\title{Validate that parallel and serial results have the same structure}
\usage{
validate_parallel_result(serial_result, parallel_result)
}
\arguments{
\item{serial_result}{A list produced by \code{parallel_map} with
\code{workers = 1}.}

\item{parallel_result}{A list produced by \code{parallel_map} with
\code{workers > 1}.}
}
\value{
\code{TRUE} (invisibly) if the structures match; otherwise an
  informative error is thrown.
}
\description{
Checks that \code{parallel_result} and \code{serial_result} have the same
length and the same element types. This function is intended for use in
tests to verify that switching to a parallel backend does not alter the
output structure.
}
\examples{
sr <- parallel_map(1:3, function(x) x * 2, workers = 1)
# In real usage parallel_result would use workers > 1; here we reuse sr.
validate_parallel_result(sr, sr)

}
