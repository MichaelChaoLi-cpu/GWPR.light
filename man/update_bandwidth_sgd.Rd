% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bandwidth_sgd.R
\name{update_bandwidth_sgd}
\alias{update_bandwidth_sgd}
\title{Update bandwidth with SGD step}
\usage{
update_bandwidth_sgd(
  bandwidth,
  gradient,
  learning_rate,
  adaptive = FALSE,
  lower = NULL,
  upper = NULL
)
}
\arguments{
\item{bandwidth}{Numeric scalar; current bandwidth.}

\item{gradient}{Numeric scalar; gradient estimate.}

\item{learning_rate}{Numeric scalar > 0.}

\item{adaptive}{Logical; `TRUE` for adaptive (integer) bandwidth.}

\item{lower}{Numeric lower bound (or `NULL`; ignored when `NULL`).}

\item{upper}{Numeric upper bound (or `NULL`; ignored when `NULL`).}
}
\value{
A numeric scalar (always positive, integer when `adaptive = TRUE`).
}
\description{
Applies a single gradient-descent update to the current bandwidth.
}
\details{
- **Fixed bandwidth**: update is performed on the log scale so that
  bandwidth stays positive:
  `new_bw = exp(log(bw) - learning_rate * gradient)`.
- **Adaptive bandwidth**: update is performed on the integer scale,
  then rounded and clipped to `[lower, upper]`.
}
\examples{
update_bandwidth_sgd(10, gradient = 2, learning_rate = 0.5,
                     adaptive = FALSE)
update_bandwidth_sgd(8L, gradient = -3, learning_rate = 1,
                     adaptive = TRUE, lower = 3, upper = 20)

}
