% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/result.R
\name{new_gwpr_fit}
\alias{new_gwpr_fit}
\title{Construct a new gwpr_fit object}
\usage{
new_gwpr_fit(
  call = NULL,
  family = NULL,
  formula = NULL,
  model = NULL,
  effect = NULL,
  bandwidth = NULL,
  search = NULL,
  global_model = NULL,
  local_results = list(),
  predictions = NULL,
  residuals = NULL,
  metrics = list(),
  spatial_results = NULL,
  metadata = list(),
  warnings = character(),
  ...
)
}
\arguments{
\item{call}{The matched call from the top-level API function, or
`NULL`.}

\item{family}{Character; model family: `"gaussian"` or `"binomial"`.}

\item{formula}{A formula object, or `NULL`.}

\item{model}{Character; panel model type: `"pooling"`, `"within"`, or
`"random"`.}

\item{effect}{Character; panel effect: `"individual"`, `"time"`,
`"two-way"`, or `"nested"`.}

\item{bandwidth}{Numeric scalar; the bandwidth used for fitting.}

\item{search}{A `gwpr_bandwidth` object, or `NULL` when bandwidth was
supplied directly.}

\item{global_model}{A summary / model object for the global (non-spatial)
reference model, or `NULL`.}

\item{local_results}{A list of per-unit local model results.}

\item{predictions}{Numeric vector of in-sample predicted values, or `NULL`.}

\item{residuals}{Numeric vector of residuals, or `NULL`.}

\item{metrics}{Named list of overall goodness-of-fit metrics.}

\item{spatial_results}{A data.frame produced by `build_spatial_results()`,
or `NULL`.}

\item{metadata}{Named list of supplementary metadata.}

\item{warnings}{Character vector of warnings accumulated during fitting.}

\item{...}{Additional named fields stored in the object.}
}
\value{
A named list of class `"gwpr_fit"`.
}
\description{
Creates a standardised result object for a fitted geographically weighted
panel regression model.
}
\examples{
fit <- new_gwpr_fit(
  family  = "gaussian",
  model   = "within",
  effect  = "individual",
  bandwidth = 100,
  metrics = list(R2 = 0.8, MSE = 0.1, RMSE = 0.316, MAE = 0.25),
  warnings = character()
)
print(fit)

}
