% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bandwidth_grid.R
\name{make_grid_candidates}
\alias{make_grid_candidates}
\title{Generate grid search bandwidth candidates}
\usage{
make_grid_candidates(lower, upper, step, adaptive)
}
\arguments{
\item{lower}{Numeric scalar.  Lower boundary (inclusive).}

\item{upper}{Numeric scalar.  Upper boundary (inclusive if reachable by
`step`).}

\item{step}{Numeric scalar > 0.  Increment between successive candidates.}

\item{adaptive}{Logical scalar.  When `TRUE`, candidate values are rounded
to the nearest positive integer and duplicates are removed.}
}
\value{
A numeric vector of candidate bandwidth values.
}
\description{
Enumerates candidate bandwidths from `lower` to (but not necessarily
reaching) `upper` with increment `step`.  All three boundary arguments
are mandatory — they are never inferred automatically.
}
\examples{
make_grid_candidates(lower = 1, upper = 5, step = 1, adaptive = FALSE)
make_grid_candidates(lower = 2, upper = 10, step = 2.5, adaptive = TRUE)

}
